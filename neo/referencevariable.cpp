#include <iostream>
using namespace std;

int main()
{
    //
    std::cout << "LETS CONTINUE FROM OPERATORS,GLOBAL,AND BASIC" << endl;

    int floatnum1, num2, doublenum3, floata, b, c; // when you are writing float or double you need to mention like here in int box and then seprately
    float num1 = 1.1;
    num2 = 2;
    double num3 = 8.78;
    float a = 1.1987654321;
    b = 2.00;
    c = 1.78;
    cout << "the value of num1 is:" << num1 << endl;
    cout << "the value of num2 is:" << num2 << endl;
    cout << "the value of num3 is:" << num3 << endl;
    cout << "the value of a is:" << a << endl;
    cout << "the value of b is:" << b << endl;
    cout << "the value of c is:" << c << endl;

    // no matters what you choose num or a,b,c or inetger only you have to do is to mention float or whtever you want ad same thing in int main tag

    return 0;
}